package io.mosip.kernel.auditmanager.config;


//import io.netty.util.ResourceLeakDetector;

public class CustomResourceLeakDetector //extends ResourceLeakDetector<Object> 
{
	/*
	 * public CustomResourceLeakDetector() { super(Object.class, Integer.MAX_VALUE);
	 * }
	 * 
	 * protected void addExclusions(String className) { // Override to prevent
	 * method lookup }
	 */}