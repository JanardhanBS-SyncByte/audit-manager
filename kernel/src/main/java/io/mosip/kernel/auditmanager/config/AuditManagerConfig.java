package io.mosip.kernel.auditmanager.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import io.mosip.kernel.auditmanager.impl.AuditHandlerImpl;
import io.mosip.kernel.auditmanager.request.AuditRequestDto;
import io.mosip.kernel.core.auditmanager.spi.AuditHandler;

@Configuration
public class AuditManagerConfig {

	/*
	 * @Bean public AuditHandler<AuditRequestDto> auditHandler() { return new
	 * AuditHandlerImpl(); }
	 */
	/*
	 * @Bean public OpenApiProperties openApiProperties() { return new
	 * OpenApiProperties(); }
	 */}